# Placeholder Prometheus configuration
# Add scrape targets and alert rules here.
global:
  scrape_interval: 15s
scrape_configs:
  - job_name: 'kubernetes-pods'
    static_configs:
      - targets: ['localhost:9090']
